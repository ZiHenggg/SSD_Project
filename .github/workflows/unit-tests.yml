name: Run PHPUnit Unit Tests for Registration and Input Validation

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    name: Run PHPUnit Tests for Registration + OTP
    runs-on: ubuntu-latest

    services:
      mysql:
        image: mysql:8.0
        env:
          MYSQL_ROOT_PASSWORD: root
          MYSQL_DATABASE: testdb
          MYSQL_USER: testuser
          MYSQL_PASSWORD: testpass
        ports:
          - 3306:3306
        options: --health-cmd="mysqladmin ping --silent" --health-interval=10s --health-timeout=5s --health-retries=3

      redis:
        image: redis
        ports:
          - 6379:6379

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.2'
          extensions: pdo_mysql, redis
          coverage: none

      - name: Install dependencies
        run: composer install --prefer-dist --no-progress

      - name: Run PHPUnit tests
        run: ./vendor/bin/phpunit --configuration phpunit.xml

      - name: ✅ Write Unit Test Summary
        run: |
          echo "### ✅ Unit Tests Summary" >> $GITHUB_STEP_SUMMARY
          echo "- StudentPageController: Input validation" >> $GITHUB_STEP_SUMMARY
          echo "- StudentControl: Session storage, password hashing, OTP generation + email" >> $GITHUB_STEP_SUMMARY
          echo "- OtpFlowTest: OTP resend generates new code & sends email" >> $GITHUB_STEP_SUMMARY
          echo "- ✔️ All tests executed via GitHub Actions with MySQL & Redis services" >> $GITHUB_STEP_SUMMARY
